language: node_js
node_js:
  - 8
deploy:
  provider: script
  script: deploy.sh
  on:
    branch: master
deploy:
  local-dir: _site
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in travis-ci.org dashboard, marked secure
  keep-history: true
  on:
    branch: master
